# Copy to .env.local for local development. Never commit real values.
# See docs/PRODUCT_GUIDE.md §31 and docs/claude/ENVIRONMENT_INVENTORY.md for context.

# --- Public (safe to bundle into client JS) ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# --- Server-only (never expose to the browser) ---
SUPABASE_SECRET_KEY=
RESEND_API_KEY=
EMAIL_FROM=
CRON_SECRET=
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=

# Added in Phase 1 once the ITM@15 Supabase project exists. Not required to boot
# the app before then — see src/lib/env.ts for what's currently enforced.
