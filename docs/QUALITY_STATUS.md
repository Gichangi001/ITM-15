# ITM@15 Quality Status

_Last updated: 2026-09-16, Experience Transformation Slice 10 (Kinshasa Grand Finale) — see `docs/PROJECT_STATE.md`'s "Experience Transformation — Slice 10" section for the full account._

**Experience Transformation Slice 10 (Kinshasa Grand Finale)**: `pnpm verify` (lint/typecheck/121 tests/build) clean; route list unchanged. The `score_events`/`player_achievements` RLS SELECT policies this relies on were independently re-confirmed via a direct `pg_policies` query against the live database before writing the code, not assumed. Same disclosed gap as Slice 9 — reaching the finale needs a player who has genuinely completed all 7 days, which doesn't exist yet in the live campaign, and there's no Playwright to simulate one.

**Experience Transformation Slice 9 (DRC flight arrival cinematic)**: `pnpm verify` (lint/typecheck/121 tests/build — 5 new tests for the African-countries content list) clean. The production `.next/static/chunks/` output was `grep`'d directly and confirmed to contain the cinematic's distinctive strings, proving it's genuinely bundled. **Not done, disclosed**: this feature is gated behind real sign-in + onboarding, so — unlike Slice 8's homepage check — there was no way to `curl`-verify the actual rendered animation this session. No Playwright/browser tool available, same gap as every slice, more consequential here than most since this is a purely visual/motion feature with no way to inspect its correctness from code alone.

**Experience Transformation Slice 8 (admin-editable journey copy)**: `pnpm verify` (lint/typecheck/116 tests/build) clean; route list unchanged. `supabase/migrations/20260916110000_journey_theme_content_fields.sql` applied live and re-confirmed via direct read-only query (all 9 rows correctly carry countryName/countryFlag/tagline/dayNumber). **Stronger than usual verification**: after switching the homepage's DRC line to read live from the database instead of the static file, re-confirmed via `curl` that it still renders correctly — proving the full live chain (anon RLS read → helper → page) genuinely works end-to-end, not just that the code compiles. The new admin "Edit copy" form itself is unverified live — no Playwright, same gap as every slice.

**Experience Transformation Slice 4 (Passport + Achievements)**: `pnpm verify` (lint/typecheck/116 tests/build) clean; route list unchanged. Live `curl` confirms `/achievements`/`/passport` still redirect unauthenticated visitors to `/login`, and the public `/passport/[slug]` route still 404s (not redirects) for an unknown slug. Per the user's explicit direction, visual verification (still no Playwright tool this session) is deferred to a later pass rather than blocking further slices.

**Experience Transformation Slice 3 (DRC opening + Kinshasa finale)**: `pnpm verify` (lint/typecheck/116 tests/build) clean; route list unchanged. The DRC cold-open line's presence was confirmed directly in the homepage's live rendered output via `curl` — the one executed check this slice got beyond a clean build. **Not done, disclosed**: the Kinshasa finale state (gated on a real Day-7-complete check) has no live player who has actually reached it yet and no Playwright tool to drive a synthetic one through all 7 missions, so it's verified by code review only. No Playwright/browser tool was available for this slice, same gap as Slices 1 and 2. Deliberately did NOT test theme activation for the other 6 countries against the live production database, since the campaign has real connected players right now and the currently-active theme (Kenya) is genuinely what they should be seeing — that verification needs a non-production context a future Playwright-equipped session can set up safely.

**Experience Transformation Slice 2 (theme engine v2 + Day 1 Kenya)**: `pnpm verify` (lint/typecheck/116 tests/build) clean; route list unchanged. `supabase/migrations/20260916100000_journey_country_themes.sql` applied live and re-confirmed via a direct read-only query (`journey_kenya` is `is_active: true`, exactly one active row); `get_advisors(security)` shows no new findings. `journey.ts`'s `themeKey` values cross-checked character-for-character against the migration's `key` column. **Not done, disclosed**: no Playwright/browser tool was available this session, and `execute_sql` was discovered to run read-only, so the day-publish → theme-activation code path (`updateGameDayStatus` → `activateThemeByKey`) was verified by code review against already-proven activation logic plus the migration's real end-state, not by an executed write test or a live browser click-through. A future session with Playwright access should publish a day as admin and watch the theme/flag/tagline change live before this is considered fully verified.

**Experience Transformation Slice 1 (de-examify the core play loop)**: `pnpm verify` (lint/typecheck/116 tests/build) clean. `next build`'s route list unchanged — no routes added/removed, only rendering/copy changed inside existing pages. Live dev-server `curl` confirms the homepage returns 200 and `/play`/`/leaderboards` still correctly 307-redirect an unauthenticated visitor to `/login`. **Not done, disclosed**: no Playwright/browser tool was available this session, so there is no visual/interaction proof of the new hero card, tap-to-choose challenge cards, or leaderboard own-row highlight on a real screen (mobile or desktop) — this matters more for a visual redesign slice than most. A future session with Playwright access should do a full live visual pass before this is considered verified beyond "builds cleanly and preserves existing auth gates."

**Phase 12 admin notification composer**: `pnpm verify` (lint/typecheck/116 tests/build — 12 new tests in `src/lib/notifications/schemas.test.ts`) clean. `/admin/notifications` confirmed in the production build's route list and confirmed to real-redirect (307 → `/login`) when hit unauthenticated via `curl`, matching every other admin route's `src/proxy.ts` gate. Schema tables re-confirmed live via `mcp__claude_ai_Supabase__list_tables` before writing app code. The recipient-resolution query was independently re-run read-only against the live database and returned the correct 3 real Kenya-based active accounts. **Not done this session, disclosed**: no Playwright/browser tool was available (not an installed dependency, no browser MCP connected) — no live click-through of the composer form, no live-inserted test notification (deliberately avoided since the live `profiles` table now holds real accounts, not only synthetic `.test` ones), no confirmation that a non-privileged role is bounced from the page in a real browser. A future session with Playwright access should complete this exact verification using fresh synthetic accounts before the phase is marked fully verified.

**Phase 20 — security review** (Product Guide §26 Phase 20): a dedicated `security-reviewer` subagent pass over Phases 15/17(completion)/18/19's full new surface area returned **BLOCK** with 3 real findings, all fixed the same session and re-verified live — see the dedicated commit and `docs/PROJECT_STATE.md`'s "Phase 20" section for the complete account. Summary: **CRITICAL** — neither the auto-graded mission-answer path nor the moderator-approval path guarded against awarding points more than once per challenge/player, an unlimited point-farming path requiring no special access; fixed, verified live (3 approved submissions for one challenge → exactly one score_events row). **HIGH** — self-registration's un-onboarded accounts had their raw email leaked as a display-name fallback on /leaderboards and /gallery, visible to every other player; fixed (generic fallback, "Newest members" filtered to onboarded accounts only), verified live. **MEDIUM** — `checkLoginMethod`/`instantJoin` had no rate limiting at all (an unauthenticated admin-role oracle, and an unlimited account-creation/email-harvesting path); fixed with a new IP-keyed Postgres rate limiter, verified live (pre-seeding hits past the threshold correctly forced the fail-safe branch). One LOW/informational item (a migration comment overstating RLS's column-scoping ability) corrected. `pnpm verify` clean after the fix commit; `get_advisors` shows no new findings.

**Phases 15/17(completion)/18/19 + realtime crash fix**: `pnpm verify` (lint/typecheck/104 tests/build) clean after every one of the 5 commits in this pass (crash fix, Phase 17 completion, Phase 15, Phase 18, Phase 19); full-repo `pnpm lint` clean throughout. Each phase verified live individually — see `docs/PROJECT_STATE.md`'s "Phases 15, 17 (completion), 18, 19" section for the exact scenario per phase (a two-browser-tab live theme switch; the previously-crashing player page loading with zero console errors; a full mission→achievement→passport-share loop; a real, freshly-cleaned-database analytics read). Phases 14 (Wally 3D) and 16 (Email) remain genuinely blocked on missing external inputs (a real 3D asset; a Resend API key) — not attempted, not faked.

**Instant sign-in** (`src/app/login/actions.ts`'s `instantJoin`, replacing the now-deleted `sendMagicLink`/`src/app/auth/callback/route.ts`): `pnpm verify` (lint/typecheck/104 tests/build) clean; `/auth/callback` confirmed gone from the build's route list. **Fully verified live this time — no disclosed gap**, unlike the emailed-link version it replaces: because sign-in now completes in one request (no email, no click, no separate device/browser to worry about), the exact real flow was driven end-to-end on both local dev and production — a brand-new email → immediate account creation → `/onboarding`, confirmed against the live database (`ACTIVE`, `must_change_password: false`, role exactly `PLAYER`); an existing account with `must_change_password` still `true` → immediate sign-in → `/play`, flag correctly cleared; the real admin email confirmed still gated behind a password field with no instant-join option shown, on both environments.

**QR codes / self-registration / country-entity self-add** (`src/components/QrCode.tsx`, `src/content/worldCountries.ts`, `src/app/onboarding/*`; `src/app/login/actions.ts` and `src/app/auth/callback/route.ts` as they existed at the time — both since replaced/removed by "Instant sign-in" above, which closed the email-click gap this paragraph originally disclosed): `pnpm verify` (lint/typecheck/104 tests/build) clean at the time, including full-repo lint (clean this time — the earlier Wally-file lint failure was resolved upstream). QR codes verified rendering on `/` and `/login` with deterministic byte-length matching a reference encode of the same URL; `/` was caught statically prerendering and fixed with `force-dynamic` before it could ship a stale build-time URL. The onboarding get-or-create logic (new profile + hardcoded PLAYER role; unseeded-country get-or-create; entity get-or-create) verified directly against the live database with the actual insert shapes the app uses. **One remaining disclosed gap**: the onboarding form's own browser interaction (would have needed a role-escalation testing shortcut this session's own permission classifier correctly refused, and that refusal was respected) — see `docs/PROJECT_STATE.md` for exactly what was verified instead.

**Phase 13 (Wally W1/W2) verification**: event/dialogue core (`priority.ts`, `resolver.ts` — 16 new unit tests) plus a real 2D prototype (`WallyProvider`, `WallySpeechBubble`, `/admin/live/wally`), verified live with three synthetic accounts (two players + one admin, all fully deleted after): real-first-name LOGIN_GREETING with deterministic highest-weight dialogue selection; a player-targeted admin message reaches only that player's already-open tab live, never the other player's; a GLOBAL message (behind a `window.confirm` gate) reaches both live; a real correct mission answer produces a Wally reaction with the exact, non-fabricated points from `score_events`. One real app bug found and fixed: `WallyProvider`'s greeting effect wrote its dedupe flag before an `await`, which under React Strict Mode's dev-mode double-invoke caused the greeting to never render at all (not just late) — fixed by deferring the flag write until after the fetch resolves. `pnpm verify` (lint/typecheck/96 tests/build) clean. Also used this pass to correct several checklist entries in `docs/PROJECT_AUDIT_CHECKLIST.md` that had gone stale since the original Phase 5 audit snapshot (e.g. "Mission creation... not started" — Phases 6-10 shipped since).

**Magic-link sign-in for non-admin accounts** (`src/app/login/actions.ts`, `src/app/auth/callback/route.ts`, `src/lib/auth/session.ts`, `src/app/login/LoginForm.tsx`/`page.tsx`, `src/app/first-login/actions.ts`): `pnpm typecheck`/`test` (96 tests)/`build` clean; `pnpm lint` scoped to these 6 files clean (the full-repo lint currently fails on an unrelated, untracked, in-progress Wally file from a concurrent session — not part of this change, not committed). Verified live on both local dev and production (`itm-15.vercel.app`): admin email → password path, real admin login works with the new password; a real non-admin test account and a wholly unknown email both → magic-link path with an identical generic response (non-enumeration holds); the callback route's failure branch (missing/invalid code) verified live. **Disclosed gap**: full click-through of an actually-emailed link could not be verified in this sandbox (no inbox access) — see `docs/PROJECT_STATE.md` for exactly why and what was verified instead. Recommend a real end-to-end check with a real inbox before relying on this for actual participants.

**Phase 12 pause/resume verification**: live Playwright E2E across the full loop (see `docs/PROJECT_STATE.md`'s "Phase 12 — Pause/resume" write-up) — server-side enforcement confirmed by direct database inspection (zero `submissions` rows from the paused attempt), not just the rendered rejection text. One real, pre-existing app bug found and fixed during this pass: the mission page's "already completed" branch was silently swallowing the correct-answer success confirmation on every submission (since Phase 6-9), not just during pause testing — fixed by having that branch compute and show the real points earned from `score_events` directly. `pnpm verify` (lint/typecheck/79 tests/build) clean. The notification-composer half of Phase 12 remains blocked on Supabase migration-apply tool access — see `docs/PROJECT_STATE.md`'s "Phase 12 — Admin notifications (blocked)".

**Phase 11 verification**: the realtime code itself was found already built/committed by a concurrent session (`07b4304`); this session independently re-verified it from scratch with a fresh live test rather than trusting the prior `pnpm verify` pass alone — see `docs/PROJECT_STATE.md`'s Phase 11 write-up for the full six-scenario, zero-reload proof (presence, mission/day publish, scoring, moderation queue, gallery, voting). Two real bugs were found and fixed **in the test script**, not the app (an ambiguous CSS selector, and several test tabs that were never actually authenticated) — disclosed rather than glossed over, since either would have produced a false "it works" if not caught.

**Prior slice** (`src/app/admin/page.tsx`, no migration): `pnpm verify` clean (lint/typecheck/79 tests/build). Live-verified with Playwright against a SUPER_ADMIN test fixture: all 5 real Quick Action links render and one was clicked through to confirm actual navigation; the 4 not-yet-built actions render as honestly-locked, non-interactive cards; the activity feed's icon map renders correctly against real `audit_logs` rows read from the database (not a fabricated example). No security-relevant surface changed — the page still re-derives every capability from `src/lib/auth/roles.ts` server-side; the Quick Actions array only controls what is *shown*, not authorization itself.

| Gate | Status | Notes |
|---|---|---|
| Lint | **Pass** | `pnpm lint` (`eslint`, `eslint-config-next` core-web-vitals + typescript configs) — clean. |
| Typecheck | **Pass** | `pnpm typecheck` = `next typegen && tsc --noEmit`. Next.js 16's typed routes (`LayoutProps<"/">` etc.) require `next typegen` to run once before a bare `tsc` resolves them — folded into the script so this isn't a trap for the next session. Passes with the newly `Database`-typed Supabase clients and all Phase 6-9 code. |
| Unit tests | **Pass** | `pnpm test` (Vitest) — 12 test files, 79 tests, all passing. Adds `src/lib/content/schemas.test.ts` (12), `src/lib/scoring/schemas.test.ts` (4), `src/lib/voting/schemas.test.ts` (4) — found already written as part of the audited work. |
| Integration tests | **Partial — covered by the live E2E pass below, not a separate integration-test suite.** No `tests/` directory or CI-runnable integration tests exist yet; every server action across every phase was verified by actually calling it through the real UI against the live database, not by a maintained automated suite a future session/CI can re-run on demand. Adding real `tests/integration/` coverage remains a gap. |
| RLS / database tests | **Pass — verified against the live project, 2026-09-13.** New migrations this session: `20260913080000_content_submission_scoring_voting.sql` (`game_days`/`missions`/`challenges`/`challenge_options`/`submissions`/`score_events`/`media_assets`/`polls`/`poll_options`/`votes`, RLS on every table from creation), `20260913082500_add_missing_votes_voter_id_index.sql` (advisor-caught FK index), `20260913083000_challenge_submissions_storage.sql` (private Storage bucket + per-uploader-folder policies). All confirmed applied live (`list_tables`/direct query, not just file presence). RLS proven correct empirically, not just by reading the policy: a player could see their own published mission/day and nobody else's draft ones; a moderator could see pending submissions across players (via service role, same pattern as every other cross-user admin read in this project); vote uniqueness held under the real UI's duplicate-attempt path. |
| E2E tests | **Pass — live, manual (not yet a committed automated suite).** Full Day-Zero-style loop run against two synthetic accounts (`e2e.super@itm15.test` SUPER_ADMIN, `e2e.player@itm15.test` PLAYER, service-role created, fully deleted after including their uploaded storage object): admin creates+publishes a SINGLE_CHOICE mission → player answers correctly → a real 100-point `score_events` row is created (checked directly against the database) → admin creates+publishes a PHOTO_UPLOAD mission → player uploads real image bytes via the browser Supabase Storage client → submission lands PENDING → admin approves it via the real moderation queue (with a working signed-URL preview) → a real 50-point `score_events` row appears → `/leaderboards` reflects the true 150-point total (individual and country) → `/gallery` shows only the approved photo → admin creates+opens a poll → player votes → a duplicate-vote attempt is correctly refused and the database confirms exactly one row → admin reveals the poll → real vote counts render → a PLAYER account is still correctly bounced from `/admin/missions`. **Plus, Phase 11**: the same loop re-run with every page opened once and never reloaded, proving realtime broadcast/presence genuinely replaces the manual refresh across all six scenarios (see PROJECT_STATE.md's Phase 11 write-up). Scripts remain scratchpad-only, not committed to `tests/e2e/` — same disclosed gap as every prior phase. |
| Build | **Pass** | `pnpm build` (`next build`, Turbopack) — compiles, typechecks, prerenders static pages, 14 dynamic (auth/data-dependent) routes including the new `/admin/missions[/new]`, `/admin/scoring`, `/admin/submissions`, `/admin/voting[/new]`, `/vote/[pollId]`, `/leaderboards`, `/gallery`. `ƒ Proxy (Middleware)` confirms `src/proxy.ts` is picked up. |
| Visual QA | **Not done for Phase 2's new pages.** Homepage/storyline/preview pages were screenshot-checked in earlier sessions (see below); the new `/login`, `/first-login`, `/admin/players/new`, `/admin/players` pages have not had a dedicated visual/mobile/accessibility pass — functionally verified via real form submission, not visually reviewed. Real gap to close before calling Phase 2 fully polished (functionally complete is not the same bar). |
| Security gate | **PASS for Phase 2** (`security-reviewer` subagent, isolated context) — no critical finding across authorization bypass, self-lockout/privilege-escalation, password handling, information disclosure, input validation, the disabled-account re-check, and audit-log coverage. One real Medium finding (redirect branches in `src/proxy.ts` dropped Supabase cookie mutations) fixed and empirically re-verified. **PASS for Phases 6-9** (manual read-through of every migration/server action this session, not a dedicated subagent pass) — one real functional gap found (`game_days` never publishable, see PROJECT_STATE), no authorization bypass found: every scoring path re-derives correctness/eligibility server-side, every admin action independently re-checks its own role capability beyond the coarse `/admin/*` gate, the `unique(poll_id, voter_id)` constraint (not just a server-action check) is the real backstop against a concurrent-vote race, and the private Storage bucket's own RLS policies (not just the server action) restrict uploads to the uploader's own uid-prefixed folder. No secrets present in the repo or `.env.example`; `SUPABASE_SECRET_KEY` never appeared in any command output or committed file. **Dedicated `security-reviewer` subagent pass for Phase 6-10 attempted separately and failed** — hit the account's monthly spend limit (HTTP 429) before returning any findings, not a pass/fail on the code itself. A second independent live-verification pass (this session, its own synthetic accounts, same full loop plus explicit Phase 10 focus: signed-URL gallery/moderation preview, unity-points-on-approval, gallery showing only approved content) reached the same conclusions as the first session's manual read-through, including directly attempting a duplicate-vote INSERT via raw SQL (bypassing the app entirely) to confirm the database constraint itself rejects it, not just the server action. A dedicated subagent pass remains a real gap until the spend limit resets. |
| Preview/production deployment | **Pass** | `https://itm-15.vercel.app` — HTTP 200. Phase 2's new routes have not yet been verified against the Vercel deployment specifically (only against local `pnpm dev`) — the live Supabase project is the same one Vercel production points at, so this is a real gap: a push to `main` would deploy Phase 2 to production without it having been checked there first. |
| Load test | Not applicable | Far ahead of current phase (Phase 20 concern). |

## Tooling health

| Check | Result |
|---|---|
| `git status` | Clean tree pending this session's commit |
| Node.js | v26.0.0 |
| npm | 11.12.1 |
| corepack | **Installed this session** (`npm install -g corepack && corepack enable`) |
| pnpm | **12.4.1**, working; `esbuild` postinstall script approved via `pnpm approve-builds` (pnpm's supply-chain policy blocks unapproved postinstall scripts by default — legitimate here, required by Vitest) |
| gh CLI | Authenticated (`Gichangi001`) |
| Vercel CLI | **v59.16.0** (upgraded this session from v54.2.0 — fixes the `vercel env add ... preview` bug noted previously), authenticated; `itm-15` project linked (`.vercel/`, gitignored) and confirmed Git-connected to this repo. Preview env vars still not added — the actual write is a secret-store write this session's permission classifier correctly declined to make unilaterally; the user has the exact script to run. |
| Supabase connector | **Project-scoped `mcp__supabase__*` tools confirmed working this session** (`ToolSearch` + live calls against `ysjjgzakswaohmnaowmv`: `list_tables`, `apply_migration`, `get_advisors`, `generate_typescript_types`, `execute_sql` all exercised successfully). The account-level `mcp__claude_ai_Supabase__*` connector remains separately capped/scoped to `soko-ai` and is unrelated to this project. |
| `.mcp.json` | `supabase` now individually exercised and confirmed healthy (see above). `vercel`/`playwright`/`memory` still not individually auth-tested beyond the account-level connector calls made directly. |
| Project skills | 5/10 created |
| `.claude/settings.json` | Scoped permissions (allow/ask/deny) + `PreToolUse` hook, both created and hook directly tested this session |
| Docker | Not installed — blocks local Supabase dev stack (`supabase start`, `db lint`, `db reset`) |
| Prisma | `prisma`/`@prisma/client` 7.10.0 installed (pinned — `prisma`'s npm `latest` tag is currently an `8.0.0-rc` pre-release). `prisma version` runs correctly. Not yet functional: `DATABASE_URL`/`DIRECT_URL` need the real DB password; introspection-only per `docs/adr/0001-prisma-alongside-supabase-migrations.md` |

## Phase 17 addendum — leaderboard online status + gallery camera capture (2026-09-13)

Built in the same working tree alongside a concurrent session's Passport/Achievements/Notifications/instant-sign-in work (see this file's other entries above). `pnpm verify` (lint/typecheck/104 tests/build) clean against the combined state of both sessions' changes. Live-verified with Playwright (synthetic accounts, fully deleted after): leaderboard "Newest members" + online dot renders without error; a real camera-capture (`capture="environment"` file input) upload flows through `/admin/gallery-moderation` into the public `/gallery`; a real `updateGameDayStatus(LIVE)` produces a real `DAY_STARTED` row in a player's own notifications inbox. New migration `20260913120000_phase17_passport_achievements_gallery.sql` applied live (via the account-level `mcp__claude_ai_Supabase__*` connector reaching the ITM-15 project directly by `project_id`, newly discovered this session), plus the previously-blocked `20260913100000_admin_notifications.sql` in the same pass — `get_advisors` run after both, one real finding (missing FK index) fixed immediately. One real, cross-cutting bug found live and fixed (by the concurrent session, root-caused jointly): a second independent `channel("game:global")` subscription crashed every player page — see `docs/PROJECT_STATE.md`'s Phase 17 addendum for the full account.

## Founder Story — "The Man Before the Group" (2026-09-14)

New route `/story/founder` (docs/ITM15_FOUNDER_STORY_OPENING_CHAPTER.md), linked from the homepage's cold-open CTA. `pnpm verify` (lint/typecheck/104 tests/build) clean against the combined tree (this work alongside the concurrent session's Phase 15 Themes / security-fix work landing at the same time). Live-verified with Playwright: desktop (1440×900) and mobile (390×844) screenshots of every scene reviewed directly for the hero image crop specifically (tuned `object-position` empirically against the real rendered size, not just estimated from the source photo — see `docs/PROJECT_STATE.md`'s write-up for why the first attempt was too aggressive a close-up); `prefers-reduced-motion` shows all content immediately; no mobile horizontal overflow; every CTA/skip control navigates correctly. A real authenticated test player reaching the origin-clue scene received a genuine `player_achievements` row and a real `analytics_events` row (confirmed directly against the database) — not just UI text; an anonymous visitor's funnel events are real rows with `player_id: null`. New migration `20260914090000_analytics_events.sql` applied live (the first real use of the table Product Guide §23 names but nothing had created until now) plus a plain data insert seeding the `origin_clue` achievement. Test account (`story.test@itm15.test`) and test analytics rows fully deleted after.

## Campaign gone live — all seven days seeded and activated (2026-09-14)

`pnpm verify` (lint/typecheck/104 tests/build) was already clean going in — no code errors to fix. A real, live, 31-page Playwright pass across every admin and player route (as a real SUPER_ADMIN and a real PLAYER account) found **zero console/network errors** anywhere in the app. The real gap was content, not code: only Day 1 existed with zero missions; Days 2-7 didn't exist as rows. Fixed via `supabase/migrations/20260914100000_seed_seven_day_content.sql` — one real mission per day, all 4 implemented challenge types covered, all 7 days + the campaign set `ACTIVE`. **Verified live end-to-end**: all 7 missions were actually answered through the real UI — 2 correct SINGLE_CHOICE answers came back `APPROVED` with real `score_events` (confirmed directly against the database); 1 deliberately-incomplete MULTIPLE_CHOICE answer came back genuinely `REJECTED`, proving server-side grading is real, not decorative; the remaining 4 (FREE_TEXT ×3, PHOTO_UPLOAD ×1, including a real storage upload) landed `PENDING`, correctly awaiting moderation. Test account and all its generated data fully deleted after — the real seeded content (missions/days/campaign) was left in place as genuine, live game content.

## Rule for future updates to this file

Never mark a gate green based on a prior commit after relevant code has changed. Record the exact commit SHA the result belongs to, and if a gate was skipped, say so explicitly rather than omitting the row.
